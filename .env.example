# Variables para el stack local de Docker (docker-compose.yml). Copia a .env y ajusta.
# Solo desarrollo local — NO usar estos valores en AWS (allá los secretos viven en SSM).
DB_USER=trackion
DB_PASSWORD=trackion
DB_NAME=trackion
JWT_SECRET=local-dev-secret-change-me
ADMIN_EMAIL=admin@trackion.local
ADMIN_PASSWORD=admin123
